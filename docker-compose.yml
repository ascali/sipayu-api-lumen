services:
  web:
    build: .
    tty: true
    ports:
      - "80:80"   # HTTP
      - "443:443" # HTTPS
    volumes:
      - ./letsencrypt:/etc/letsencrypt # Volume untuk menyimpan sertifikat
      - ./ssl:/etc/ssl # Volume untuk menyimpan file SSL
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - APP_URL=http://localhost
      - DB_CONNECTION=pgsql
      - DB_HOST=27.112.78.145
      - DB_PORT=5432
      - DB_DATABASE=sipayu_db
      - DB_USERNAME=postgres
      - DB_PASSWORD=k3s24fGWWQ6FKZnRWDbAaBhiYWB6jxIZLtRNAYGwW4Ea8GepKXyBWQqDRBwpwrea
      - JWT_SECRET=YPlJAj6L74PHwVZQDucBulT77xTb53wGTjysOI1YsReyD59SiMxXlFl6BlKYlHgD
      - MAIL_MAILER=smtp
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=465
      - MAIL_USERNAME=disparaindramayu@gmail.com
      - MAIL_PASSWORD=vrfomwvazgcgcwlz
      - MAIL_ENCRYPTION=ssl
      - MAIL_FROM_ADDRESS=disparaindramayu@gmail.com
      - MAIL_FROM_NAME=SIPAYU
    networks:
      - lumen-network

networks:
  lumen-network: